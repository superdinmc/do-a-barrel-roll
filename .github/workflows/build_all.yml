name: Build All

on: [pull_request, push, workflow_call]

jobs:

  build_1_19:
    uses: enjarai/do-a-barrel-roll/.github/workflows/build.yml@main
    with:
      branch: main-architectury-dev
      java_version: 17